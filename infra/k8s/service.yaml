apiVersion: v1
kind: Service
metadata:
  name: financial-analytics-api
  namespace: financial-analytics
spec:
  type: ClusterIP
  selector:
    app: financial-analytics-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
